import BorrowApplicationForm from "@/components/BorrowApplicationForm";
import Layout from "@/components/Layout";

export default function BorrowPage() {
  return (
    <Layout>
      <BorrowApplicationForm />
    </Layout>
  );
}
